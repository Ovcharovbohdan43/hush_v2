# Railway configuration
# This file tells Railway to deploy only the server directory

# Root directory for the service
root = "server"

# Build command
build = "cargo build --release"

# Start command
start = "cargo run --release"

# Port (Railway will set PORT env var)
port = 3001

