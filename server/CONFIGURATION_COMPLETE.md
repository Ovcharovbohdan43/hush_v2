# ✅ Конфигурация завершена

## Данные Neon получены и готовы к использованию

### Ваши данные подключения:
- **Neon Project URL**: https://console.neon.tech
- **Database Host**: ep-silent-glitter-ahvalyxw.us-east-1.aws.neon.tech
- **Database Name**: neondb (по умолчанию)
- **User**: <ваш Neon пользователь, напр. `neondb_owner`>
- **Password**: <пароль от Neon>
- **REST API**: https://ep-silent-glitter-ahvalyxw.apirest.c-3.us-east-1.aws.neon.tech/neondb/rest/v1

> Все реальные значения подставьте из консоли Neon. Обязательно храните их в безопасном месте.

## 📝 Что нужно сделать сейчас:

### 1. Создайте файл `.env` в директории `server/`

```env
DATABASE_URL=postgresql://<username>:<password>@ep-silent-glitter-ahvalyxw.us-east-1.aws.neon.tech/neondb?sslmode=require
PORT=3001
API_BASE_URL=http://localhost:3001
JWT_SECRET=your-secret-key-change-in-production-min-32-chars-please-change-this
JWT_EXPIRES_IN=3600
REFRESH_TOKEN_EXPIRES_IN=604800
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-username@smtp-brevo.com
SMTP_PASSWORD=your-smtp-password-here
SMTP_FROM=noreply@hush.example
HUSH_DOMAIN=hush.example
# Необязательно: REST API Neon Data
# NEON_REST_ENDPOINT=https://ep-silent-glitter-ahvalyxw.apirest.c-3.us-east-1.aws.neon.tech/neondb/rest/v1
# NEON_API_KEY=your-neon-api-key
```

**Важно**:
- Используйте реальные `<username>` и `<password>` из Neon.
- Всегда добавляйте `?sslmode=require` — Neon требует TLS.

### 2. Запустите сервер

```powershell
cd server
cargo run
```

### 3. Проверьте работу

```powershell
curl http://localhost:3001/health
```

Должен вернуть: `OK`

## 🔧 Возможные проблемы и решения

### Ошибка: `No such host is known (os error 11001)`
- Проверьте DNS: `nslookup ep-silent-glitter-ahvalyxw.us-east-1.aws.neon.tech`
- Убедитесь, что DNS резолвит IPv4/IPv6. Neon отдаёт и IPv4, и IPv6.
- Если доступ блокируется, используйте VPN или настройте корпоративный DNS/Firewall для доступа.

### Ошибка: `certificate verify failed`
- Убедитесь, что используете `sslmode=require`.
- При необходимости скачайте корневой сертификат из Neon и укажите `sslrootcert`.

### Ошибка миграций
- Убедитесь, что строка подключения корректна и база доступна.
- В консоли Neon проверьте наличие `neondb` и прав пользователя.

## 📚 Документация

- `ENV_SETUP.md` — актуальный `.env` шаблон для Neon
- `NEON_SETUP.md` — расширенные инструкции по настройке (см. ниже)
- `API_EXAMPLES.md` — примеры использования API
- `README.md` — общая документация сервера

## ✅ Готово к запуску!

После создания `.env` и проверки подключения сервер готов к работе. Endpoint `/api/v1/...` используют ту же схему, что и ранее, только база — Neon.

## 🎯 Следующие шаги после запуска:

1. Протестируйте регистрацию и логин.
2. Создайте алиас и убедитесь, что данные сохраняются в Neon.
3. Проверьте отправку писем (SMTP).
4. При необходимости подключите Neon REST API (для аналитики/дашбордов).

