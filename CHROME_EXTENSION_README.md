# Chrome Extension - Готовность к запуску

## ✅ Что готово:

1. **Manifest V3** (`manifest.json`) - создан
2. **Popup компонент** (`src/popup.tsx`) - создан и подключен к API
3. **Options страница** (`src/options.tsx`) - создана с аутентификацией
4. **Background service worker** (`src/background.ts`) - создан
5. **Content script** (`src/content.ts`) - создан для инжекта на страницы
6. **Конфигурация Vite** - обновлена для сборки расширения
7. **API клиент** - обновлен для работы с chrome.storage
8. **AuthContext** - обновлен для работы с chrome.storage

## ⚠️ Что нужно сделать перед запуском:

### 1. Создать иконки расширения

Создайте PNG иконки размером:
- `icons/icon16.png` - 16x16 пикселей
- `icons/icon48.png` - 48x48 пикселей  
- `icons/icon128.png` - 128x128 пикселей

Можно использовать:
- Figma
- Canva
- Или любой графический редактор

Иконка должна быть с прозрачным фоном и содержать символ щита (Shield) или логотип Hush.

### 2. Собрать расширение

```bash
npm install
npm run build
```

### 3. Скопировать файлы в build

После сборки нужно скопировать:
- `manifest.json` → `build/manifest.json`
- `icons/` → `build/icons/`

### 4. Установить в Chrome

1. Откройте `chrome://extensions/`
2. Включите "Режим разработчика" (Developer mode)
3. Нажмите "Загрузить распакованное расширение" (Load unpacked)
4. Выберите папку `build`

## 📝 Примечания:

- Расширение использует `http://localhost:3001` как API endpoint по умолчанию
- Можно изменить API URL в настройках расширения (Options page)
- Для работы расширения нужен запущенный бэкенд сервер
- Frontend веб-приложение должно быть доступно на `http://localhost:3000`

## 🔧 Настройка API URL в расширении:

API URL можно настроить в Options page расширения. По умолчанию используется `http://localhost:3001`.

