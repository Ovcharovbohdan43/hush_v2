# 🚀 Быстрый старт ngrok для Hush

## ngrok найден и настроен

ngrok находится в: `C:\dev\Telegram bot reminder\ngrok.exe`

## Запуск ngrok туннеля

Для запуска туннеля к вашему серверу выполните:

```powershell
& "C:\dev\Telegram bot reminder\ngrok.exe" http 3001
```

Или добавьте ngrok в PATH для удобства:

```powershell
# Временно для текущей сессии
$env:Path += ";C:\dev\Telegram bot reminder"

# Постоянно (требует прав администратора)
[Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\dev\Telegram bot reminder", "User")
```

После добавления в PATH можно использовать просто:
```powershell
ngrok http 3001
```

## Что делать дальше

1. **Запустите сервер:**
   ```powershell
   cd C:\dev\HashV2\server
   cargo run
   ```

2. **В другом терминале запустите ngrok:**
   ```powershell
   & "C:\dev\Telegram bot reminder\ngrok.exe" http 3001
   ```

3. **Скопируйте URL из ngrok:**
   ```
   Forwarding: https://abc123.ngrok.io -> http://localhost:3001
   ```

4. **Настройте webhook в Brevo:**
   - URL: `https://abc123.ngrok.io/api/v1/incoming/brevo`
   - Событие: "Inbound emails"

5. **Проверьте доступность:**
   Откройте в браузере: `https://abc123.ngrok.io/api/v1/incoming/test`
   Должен вернуться JSON с `"status": "ok"`

## Важно

- ngrok туннель должен быть запущен **все время**, пока вы тестируете
- Если туннель закрывается, URL меняется, и нужно обновить webhook в Brevo
- Для production используйте статический домен или VPS

