# 🔍 Диагностика проблемы: Письма не пересылаются

## Проблема
- Service worker пуст (нет логов)
- Создано 7 алиасов
- Письма не приходят на target email

## Основная причина
**Письма не доходят до webhook endpoint**, потому что:
1. Webhook не настроен в Brevo
2. Или сервер работает на `localhost:3001`, который недоступен из интернета
3. Или webhook настроен неправильно

## ✅ Шаг 1: Проверьте логи background script

1. Откройте Chrome DevTools:
   - `chrome://extensions/`
   - Найдите "Hush - Email Protection"
   - Нажмите "service worker" (или "Inspect views: service worker")
   - Откроется консоль с логами background script

2. Создайте новый алиас через расширение
3. Проверьте логи - должны быть сообщения:
   ```
   Background: Received message
   Background: Creating alias...
   Background: Got access_token from storage
   Background: Using API URL
   Background: Alias created successfully
   ```

## ✅ Шаг 2: Проверьте настройку webhook в Brevo

### Важно: Webhook должен быть доступен из интернета!

Если ваш сервер работает на `localhost:3001`, webhook **НЕ БУДЕТ РАБОТАТЬ**, потому что Brevo не может достучаться до localhost.

### Решения:

#### Вариант А: Используйте ngrok или подобный сервис (для тестирования)

1. Установите ngrok: https://ngrok.com/
2. Запустите туннель:
   ```powershell
   ngrok http 3001
   ```
3. Скопируйте URL (например: `https://abc123.ngrok.io`)
4. Настройте webhook в Brevo на: `https://abc123.ngrok.io/api/v1/incoming/brevo`

#### Вариант Б: Разверните сервер на публичном сервере

Используйте VPS (DigitalOcean, AWS, etc.) или сервис типа Railway/Render.

### Настройка webhook в Brevo:

1. Войдите в панель Brevo: https://app.brevo.com/
2. Перейдите в **Settings → Webhooks**
3. Нажмите **"Add a new webhook"**
4. Выберите событие: **"Inbound emails"** (или "Inbound email processed")
5. URL webhook: `https://your-domain.com/api/v1/incoming/brevo`
   - Замените `your-domain.com` на ваш публичный URL
6. Сохраните webhook

## ✅ Шаг 3: Проверьте логи сервера

После настройки webhook, когда письмо придет на алиас, в логах сервера должны появиться:

```
=== BREVO WEBHOOK RECEIVED ===
From: ...
To: ...
=== INCOMING EMAIL START ===
```

**Если этих логов нет** - webhook не настроен или недоступен.

## ✅ Шаг 4: Проверьте доступность endpoint

Проверьте, что endpoint доступен:

```powershell
# Если используете ngrok
curl https://your-ngrok-url.ngrok.io/api/v1/incoming/brevo

# Должен вернуть ошибку (это нормально, нужен POST с данными)
# Но главное - endpoint должен быть доступен
```

## ✅ Шаг 5: Проверьте настройки безопасности webhook

В файле `.env` сервера проверьте:

```env
# Для разработки можно отключить проверку безопасности
WEBHOOK_SECURITY_ENABLED=false

# Или настройте секрет для Brevo
BREVO_WEBHOOK_SECRET=your-secret-key
```

Если `WEBHOOK_SECURITY_ENABLED=true`, но секрет не настроен, webhook будет отклонен.

## ✅ Шаг 6: Проверьте домен алиасов

Убедитесь, что в `.env` правильно указан домен:

```env
HUSH_DOMAIN=hush.autos  # или ваш реальный домен
```

И что этот домен настроен в Brevo для получения входящих писем.

## 🔧 Быстрая проверка

1. **Проверьте background script:**
   - Откройте `chrome://extensions/`
   - Найдите расширение
   - Нажмите "service worker"
   - Создайте алиас
   - Должны быть логи

2. **Проверьте webhook:**
   - Откройте Brevo → Settings → Webhooks
   - Убедитесь, что webhook настроен и активен
   - URL должен быть публичным (не localhost)

3. **Проверьте сервер:**
   - Сервер должен быть доступен из интернета
   - Endpoint `/api/v1/incoming/brevo` должен отвечать
   - В логах должны появляться сообщения при получении webhook

## 📝 Чеклист

- [ ] Background script логирует создание алиасов
- [ ] Webhook настроен в Brevo
- [ ] Webhook URL публичный (не localhost)
- [ ] Сервер доступен из интернета
- [ ] Домен алиасов настроен в Brevo для получения писем
- [ ] `WEBHOOK_SECURITY_ENABLED=false` или секрет настроен
- [ ] В логах сервера появляются сообщения при получении webhook

## 🚨 Самая частая проблема

**Сервер работает на `localhost:3001`, а webhook настроен на `http://localhost:3001/api/v1/incoming/brevo`**

Это **НЕ РАБОТАЕТ**, потому что Brevo не может достучаться до вашего локального компьютера.

**Решение:** Используйте ngrok или разверните сервер на публичном сервере.

