# 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА: Письма не пересылаются

## Текущая ситуация
- ✅ Background script работает (видно логи storage)
- ✅ Алиасы создаются успешно (7 алиасов создано)
- ❌ Письма НЕ пересылаются на target email
- ❌ В логах сервера нет записей о получении webhook

## 🔍 Диагностика

### Шаг 1: Проверьте доступность webhook endpoint

Откройте в браузере или выполните curl:

```powershell
# Если сервер на localhost
curl http://localhost:3001/api/v1/incoming/test

# Должен вернуть:
# {"status":"ok","message":"Webhook endpoint is accessible",...}
```

**Если не работает** - сервер не запущен или недоступен.

### Шаг 2: Проверьте, доступен ли сервер из интернета

**ВАЖНО:** Если сервер работает на `localhost:3001`, Brevo **НЕ СМОЖЕТ** отправить webhook, потому что `localhost` недоступен из интернета.

#### Решение: Используйте ngrok

1. **Установите ngrok:**
   - Скачайте с https://ngrok.com/download
   - Или через PowerShell: `choco install ngrok`

2. **Запустите туннель:**
   ```powershell
   ngrok http 3001
   ```

3. **Скопируйте URL:**
   ```
   Forwarding: https://abc123.ngrok.io -> http://localhost:3001
   ```

4. **Используйте этот URL для webhook:**
   ```
   https://abc123.ngrok.io/api/v1/incoming/brevo
   ```

### Шаг 3: Настройте webhook в Brevo

1. Войдите в Brevo: https://app.brevo.com/
2. Перейдите в **Settings → Webhooks**
3. Нажмите **"Add a new webhook"**
4. Выберите событие: **"Inbound emails"** или **"Inbound email processed"**
5. URL: `https://your-ngrok-url.ngrok.io/api/v1/incoming/brevo`
   - Замените `your-ngrok-url` на ваш реальный ngrok URL
6. Сохраните webhook

### Шаг 4: Отключите проверку безопасности (для тестирования)

В файле `server/.env` добавьте:

```env
WEBHOOK_SECURITY_ENABLED=false
```

Перезапустите сервер после изменения `.env`.

### Шаг 5: Проверьте настройку домена в Brevo

1. В Brevo перейдите в **Settings → Senders & IP**
2. Убедитесь, что ваш домен (например, `hush.autos`) настроен для получения входящих писем
3. Проверьте, что домен верифицирован

### Шаг 6: Проверьте переменные окружения

В файле `server/.env` должны быть:

```env
# Домен для алиасов (должен совпадать с доменом в Brevo)
HUSH_DOMAIN=hush.autos

# SMTP настройки (для отправки писем)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=noreply@hush.autos  # Должен быть верифицирован в Brevo

# Безопасность webhook (отключить для тестирования)
WEBHOOK_SECURITY_ENABLED=false
```

### Шаг 7: Проверьте логи сервера

После настройки webhook и отправки тестового письма на алиас, в логах сервера должны появиться:

```
=== BREVO WEBHOOK RECEIVED ===
From: ...
To: ...
Subject: ...
=== INCOMING EMAIL START ===
```

**Если этих логов нет** - webhook не настроен или недоступен.

## 🔧 Быстрая проверка

1. **Запустите ngrok:**
   ```powershell
   ngrok http 3001
   ```

2. **Скопируйте URL** (например: `https://abc123.ngrok.io`)

3. **Настройте webhook в Brevo:**
   - URL: `https://abc123.ngrok.io/api/v1/incoming/brevo`
   - Событие: "Inbound emails"

4. **Отключите проверку безопасности:**
   ```env
   WEBHOOK_SECURITY_ENABLED=false
   ```

5. **Перезапустите сервер**

6. **Отправьте тестовое письмо** на один из ваших алиасов

7. **Проверьте логи сервера** - должны появиться сообщения о получении webhook

## 📝 Чеклист

- [ ] Сервер запущен и доступен на `localhost:3001`
- [ ] ngrok запущен и туннель активен
- [ ] Webhook настроен в Brevo с правильным URL
- [ ] `WEBHOOK_SECURITY_ENABLED=false` в `.env`
- [ ] Домен алиасов настроен в Brevo для получения писем
- [ ] SMTP настройки правильные
- [ ] Target email верифицирован
- [ ] В логах сервера появляются сообщения при получении webhook

## 🚨 Если ничего не помогает

1. Проверьте, что домен алиасов (например, `hush.autos`) настроен в Brevo для получения входящих писем
2. Проверьте, что DNS записи для домена настроены правильно (MX записи)
3. Убедитесь, что сервер доступен из интернета (не только localhost)
4. Проверьте логи Brevo - возможно, там есть информация об ошибках webhook

