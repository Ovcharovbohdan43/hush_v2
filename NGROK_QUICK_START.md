# 🚀 Запуск ngrok для Hush

## Быстрый способ (с полным путем)

```powershell
& "C:\dev\Telegram bot reminder\ngrok.exe" http 3001
```

## Добавить ngrok в PATH (рекомендуется)

### Вариант 1: Временно для текущей сессии PowerShell

```powershell
$env:Path += ";C:\dev\Telegram bot reminder"
ngrok http 3001
```

### Вариант 2: Постоянно (для текущего пользователя)

```powershell
[Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\dev\Telegram bot reminder", "User")
```

После этого перезапустите PowerShell и используйте:
```powershell
ngrok http 3001
```

## Что делать после запуска ngrok

1. **Скопируйте URL** из вывода ngrok:
   ```
   Forwarding: https://abc123.ngrok.io -> http://localhost:3001
   ```

2. **Настройте webhook в Brevo:**
   - URL: `https://abc123.ngrok.io/api/v1/incoming/brevo`
   - Событие: "Inbound emails"

3. **Проверьте доступность:**
   Откройте: `https://abc123.ngrok.io/api/v1/incoming/test`

4. **Отправьте тестовое письмо** на алиас и проверьте логи сервера

## Важно

- ngrok должен быть запущен **в отдельном окне терминала**
- Не закрывайте окно ngrok, пока тестируете
- URL меняется при каждом перезапуске ngrok (если нет платного аккаунта)

