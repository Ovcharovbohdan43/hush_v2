# 🔧 Переменные окружения для Railway

## Обязательные переменные (минимум 5-6)

### 1. DATABASE_URL
**Автоматически** - Railway создаст при добавлении PostgreSQL базы данных.
Если добавляете вручную:
```
postgresql://user:password@host:5432/database?sslmode=require
```

### 2. PORT
**Автоматически** - Railway установит сам. Не нужно указывать вручную.

### 3. JWT_SECRET ⚠️ ОБЯЗАТЕЛЬНО
Случайная строка минимум 32 символа для подписи JWT токенов.
```
JWT_SECRET=your-super-secret-key-min-32-chars-change-this-in-production
```

### 4. SMTP_FROM
Email адрес отправителя (используется в Mailgun API, должен быть верифицирован):
```
SMTP_FROM=noreply@hush.autos
```

### 5. MAILGUN_API_KEY
HTTP API ключ из Mailgun:
```
MAILGUN_API_KEY=key-xxxxxxxxxxxxxxxxxxxxxx
```

### 6. MAILGUN_DOMAIN
Домен Mailgun, от имени которого отправляем письма:
```
MAILGUN_DOMAIN=hush.autos
```

### 7. (Опционально) MAILGUN_API_BASE_URL
По умолчанию используется `https://api.mailgun.net`. Для EU-региона укажите:
```
MAILGUN_API_BASE_URL=https://api.eu.mailgun.net
```

### 8. HUSH_DOMAIN
Домен для алиасов:
```
HUSH_DOMAIN=hush.autos
```

### 9. API_BASE_URL ⚠️ ВАЖНО
Публичный URL вашего Railway сервиса (после деплоя):
```
API_BASE_URL=https://your-project-name.railway.app
```
**Замените `your-project-name` на реальный URL из Railway!**

### 10. WEBHOOK_SECURITY_ENABLED (для тестирования)
Отключите проверку безопасности webhook:
```
WEBHOOK_SECURITY_ENABLED=false
```

## Необязательные (но рекомендуемые)

### JWT_EXPIRES_IN
Время жизни access token в секундах (по умолчанию 3600 = 1 час):
```
JWT_EXPIRES_IN=3600
```

### REFRESH_TOKEN_EXPIRES_IN
Время жизни refresh token в секундах (по умолчанию 604800 = 7 дней):
```
REFRESH_TOKEN_EXPIRES_IN=604800
```

## Минимальный набор (5 обязательных):

1. ✅ `JWT_SECRET` - обязательно!
2. ✅ `SMTP_FROM` - адрес отправителя
3. ✅ `MAILGUN_API_KEY` - ключ API Mailgun
4. ✅ `MAILGUN_DOMAIN` - домен Mailgun
5. ✅ `HUSH_DOMAIN` - домен алиасов
6. ✅ `API_BASE_URL` - ваш Railway URL
7. ✅ `WEBHOOK_SECURITY_ENABLED=false` - для тестирования

**DATABASE_URL и PORT** Railway установит автоматически при добавлении PostgreSQL.

## Как добавить в Railway:

1. В Railway проекте откройте ваш сервис
2. Перейдите в **Variables** (или **Environment**)
3. Добавьте каждую переменную:
   - Нажмите **+ New Variable**
   - Введите имя (например, `JWT_SECRET`)
   - Введите значение
   - Сохраните

4. После добавления всех переменных Railway автоматически перезапустит сервис

## Проверка:

После добавления переменных проверьте:
```bash
curl https://your-railway-url.railway.app/health
```

Должен вернуть: `OK`

## Важно:

- `API_BASE_URL` должен быть **публичным URL** вашего Railway сервиса
- `SMTP_FROM` должен быть **верифицирован** в Mailgun
- `MAILGUN_API_KEY` и `MAILGUN_DOMAIN` выдаются в Mailgun
- `JWT_SECRET` должен быть **случайной строкой** минимум 32 символа

